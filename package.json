{
  "name": "dondeai-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "npx serve . -l 3000 --no-clipboard",
    "preview": "npm run build && npx serve dist -l 3000 --no-clipboard"
  },
  "dependencies": {
    "playwright": "^1.58.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
